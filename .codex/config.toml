# RepoOS v0.1.0 intentionally sets no project-specific Codex configuration.
# Repository policy belongs in AGENTS.md and RepoOS-owned schemas. User-global
# preferences remain outside this public repository and require separate
# authorization before RepoOS may render or install them.
